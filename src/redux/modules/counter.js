// 초기 상태값
const initialState = {
  number: 0,
};

// 리듀서: 함수
// 인자값: state, action
const counter = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default counter;
